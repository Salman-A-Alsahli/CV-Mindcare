# NPM Configuration for CV-Mindcare Frontend
# Optimized for ARM64 platforms (Raspberry Pi) but works on all architectures

# Force installation of optional dependencies (helps with Rollup native modules)
optional=true

# Increase timeout for slower ARM64 platforms
# ARM64 systems like Raspberry Pi may need more time to compile native modules
fetch-timeout=600000          # 10 minutes (default: 300000)
fetch-retries=5               # Retry up to 5 times (default: 2)
fetch-retry-mintimeout=20000  # Minimum retry delay: 20 seconds (default: 10000)
fetch-retry-maxtimeout=120000 # Maximum retry delay: 2 minutes (default: 60000)

# Cache settings
cache-min=3600                # Minimum cache time: 1 hour
prefer-online=true            # Check for updates online first

# Note: Platform and architecture are auto-detected by npm
# The setup-frontend.sh script handles ARM64-specific flags
