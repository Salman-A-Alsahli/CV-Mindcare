# CV-Mindcare Requirements
# 
# This project has modular dependencies to reduce installation size and complexity.
# Choose the appropriate installation based on your needs:
#
# 1. Base Installation (Required - ~500MB):
#    pip install -r requirements-base.txt
#
# 2. ML/AI Features (Optional - ~2GB additional):
#    pip install -r requirements-ml.txt
#
# 3. Development Tools (Optional):
#    pip install -r requirements-dev.txt
#
# Full Installation (all features):
#    pip install -r requirements-base.txt -r requirements-ml.txt -r requirements-dev.txt
#
# Or install this file for backward compatibility (includes everything):
#    pip install -r requirements.txt

# Include all requirements (backward compatibility)
-r requirements-base.txt
-r requirements-ml.txt
-r requirements-dev.txt
